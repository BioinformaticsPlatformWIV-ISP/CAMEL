import os

OUTPUT_CONTAMINATION_CHECK_REPORT = os.path.join('contamination_check', 'report', 'html.io')
OUTPUT_CONTAMINATION_CHECK_REPORT_EMPTY = os.path.join('contamination_check', 'report', 'html-empty.io')
OUTPUT_CONTAMINATION_SUMMARY = os.path.join('contamination_check', 'summary', 'summary_out.tsv')
